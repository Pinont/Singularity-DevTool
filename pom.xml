<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>singularityplugin</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>SingularityPlugin</name>

    <properties>
        <!-- v2 line: tracks SingularityLib rework/v2 branch. NOT published yet,
             so resolution fails until the lib publishes 2.0.0-SNAPSHOT to
             GitHub Packages. Accepted for this pass. -->
        <singularity.version>2.0.0-SNAPSHOT</singularity.version>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.3</version>
                <configuration>
                    <!-- Bootstrap model: SingularityLib ships as its own server
                         plugin and must NEVER be bundled into this jar. -->
                    <artifactSet>
                        <excludes>
                            <exclude>io.github.pinont:singularitylib</exclude>
                        </excludes>
                    </artifactSet>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <repositories>
        <repository>
            <id>papermc-repo</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <!-- GitHub Packages: primary home for SingularityLib artifacts.
             Concrete repo path required (GitHub Packages has no wildcard
             group URLs); matches SingularityLib distributionManagement. -->
        <!-- Public token-free registry (GitHub Pages via raw.githubusercontent).
             See Pinont/singularity-maven. -->
        <repository>
            <id>singularity</id>
            <url>https://maven.pinont.me/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
        </repository>
        <!-- (jitpack.io legacy repo removed — lib v2 publishes to Maven Central
             for releases and maven.pinont.me for snapshots.) -->
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>[26.2.build,)</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.github.pinont</groupId>
            <artifactId>singularitylib</artifactId>
            <version>${singularity.version}</version>
            <!-- Bootstrap model: lib is a standalone server plugin joined via
                 classpath; never bundled (see shade excludes above). -->
            <scope>provided</scope>
        </dependency>
        <!-- Test dependencies (MockBukkit matching Paper 26.2) -->
        <dependency>
            <groupId>org.mockbukkit.mockbukkit</groupId>
            <artifactId>mockbukkit-v26.2</artifactId>
            <version>4.116.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>2.0.12</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
